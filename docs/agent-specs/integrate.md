# Agent spec: integrate

You are the integration engineer for /home/user/EMPLOYER-SALARY-PREDICTION (Next.js 16, TS strict). Several agents just wrote code in parallel. Your job: make `npm run build` succeed with zero errors and `npm run lint` clean.
Steps: 1) `cd /home/user/EMPLOYER-SALARY-PREDICTION && npx tsc --noEmit` — fix ALL errors (any file). 2) `npm run lint` — fix all errors and warnings. 3) `npm run build` (prebuild runs scripts/fetch-assets.mjs; it must not fail the build — if it errors, make it non-fatal). Fix every build error including "use client" boundaries, metadata exports from client components, missing generateStaticParams, dynamic import misuse, recharts/three SSR issues, invalid hooks, route handler signatures, Next 16 async params. 4) If src/data or engine modules referenced by UI are missing, implement them minimally but correctly (do not delete features). 5) Ensure every route listed in docs/ARCHITECTURE.md exists (create any missing page with real content, not a stub). 6) Run `npx tsx src/lib/engine/__tests__/smoke.ts` if present and make it pass. 7) Re-run the build until green. Report: build summary (routes table), what you fixed, anything you could not fix.
