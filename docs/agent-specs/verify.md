# Agent spec: verify

Smoke-test the built app at /home/user/EMPLOYER-SALARY-PREDICTION with Playwright (Chromium is at /opt/pw-browsers, PLAYWRIGHT_BROWSERS_PATH is set; use the `playwright` npm package — install it as a devDependency with `npm i -D playwright --no-audit --no-fund` if missing, do not run "playwright install").
1) Build if .next is stale: `npm run build`. Start `npx next start -p 3123` in the background (write logs to /tmp/paylens-start.log), wait for it to answer.
2) Write /tmp/paylens-smoke.mjs that visits, with a 1366x900 viewport, EVERY route: /, /analyze, /pricing, /for-recruiters, /salaries, the first two guide slugs from /salaries links, /blog and its first post, /about, /help, /contact, /privacy, /terms, /auth/signin, /auth/signup, /dashboard, /dashboard/market-value, /dashboard/improve, /dashboard/offer-evaluator, /dashboard/compare, /dashboard/job-search, /dashboard/pipeline, /dashboard/career-growth, /dashboard/insights, /dashboard/resumes, /dashboard/chat, /dashboard/settings, /this-route-does-not-exist (expect 404 page). For each: status < 500, no uncaught page errors, collect console errors (ignore favicon 404 and WebGL/swiftshader warnings), check the page has an <h1>.
3) Functional flows in the same script: on /analyze click "Use sample resume" (find by text), submit, wait for navigation to /dashboard/market-value, assert a "$" amount appears and a percentile label appears. Then /dashboard/offer-evaluator: fill title "Senior Product Manager", location "Austin, US", base 105000, submit, assert a verdict text ("Below market"/"At market"/"Above market") appears. Then /dashboard/job-search: assert ≥3 job cards render and click the first "Save"; go to /dashboard/settings and switch the plan to Hunter (find the control); go to /dashboard/pipeline and assert the saved card exists in the Saved column. Take screenshots of /, the hero after scrolling 800px, the watch chapter after scrolling to it, /dashboard/market-value, /dashboard/pipeline into /tmp/shots/*.png.
4) Report a table route → status/errors, the flow results, and paste the first 3 console errors per failing page. Return JSON-ish summary at the end: {"passed": n, "failed": n, "failures": [...]} in your final message. Kill the server when done.
