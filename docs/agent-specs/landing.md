# Agent spec: landing


You are one of several engineers building "PayLens" in parallel inside /home/user/EMPLOYER-SALARY-PREDICTION (a Next.js 16.3 App Router app, React 19, TypeScript strict, Tailwind v4, src/ directory, "@/..." alias). Other agents are editing OTHER files at the same time.

READ FIRST (do not skip): /home/user/EMPLOYER-SALARY-PREDICTION/docs/ARCHITECTURE.md, /home/user/EMPLOYER-SALARY-PREDICTION/docs/design-brief.md, /home/user/EMPLOYER-SALARY-PREDICTION/docs/payscope-feature-catalog.md, /home/user/EMPLOYER-SALARY-PREDICTION/src/lib/types.ts, /home/user/EMPLOYER-SALARY-PREDICTION/src/lib/plans.ts, /home/user/EMPLOYER-SALARY-PREDICTION/src/lib/store.ts, /home/user/EMPLOYER-SALARY-PREDICTION/src/lib/engine/index.ts, /home/user/EMPLOYER-SALARY-PREDICTION/src/app/globals.css, /home/user/EMPLOYER-SALARY-PREDICTION/src/components/ui/index.ts (and the files it exports), /home/user/EMPLOYER-SALARY-PREDICTION/src/components/layout/Nav.tsx, /home/user/EMPLOYER-SALARY-PREDICTION/src/data/types.ts.

HARD RULES
- Only create/edit the files listed under YOUR FILES. Never edit files owned by others (types.ts, store.ts, plans.ts, globals.css, layout.tsx, Nav, Footer, ui primitives are FROZEN — if you truly need a new shared helper, create it inside your own folder).
- Write every file complete in one go; no placeholder TODOs, no "coming soon" stubs, no lorem ipsum. Every feature must actually work with the deterministic engine (no network needed).
- TypeScript strict: no `any`, no unused vars/imports (eslint is strict: eslint-config-next core-web-vitals + typescript). Prefer `import type`.
- Client components need "use client" at the top; anything using hooks, zustand (useApp), framer-motion, recharts, three, dnd-kit, window, or event handlers must be a client component. Server components are the default for pages that only render static content. Pages must export `metadata` only from SERVER components (so split a page into a server page.tsx that renders a client component when you need both).
- Zustand store persists to localStorage: read `hydrated` from useApp and render a small skeleton until true to avoid hydration mismatch. Use `useActiveAnalysis()` for the active resume + analysis.
- Money formatting: formatMoney(amount, currency) from "@/lib/format". Plan gating: <Gate feature="...">children</Gate> from "@/components/ui/Gate" and can(plan, feature) from "@/lib/plans".
- Tailwind v4 tokens available as classes: bg-bg, bg-bg-2, bg-panel, bg-panel-2, border-line, border-line-2, text-fg, text-muted, text-dim, text-cyan, bg-cyan, text-gold, bg-gold, text-ember, bg-ember, text-green, text-red, text-amber, rounded-xl2, shadow-glow-cyan, shadow-glow-gold, shadow-panel; utility classes: .display (Bebas condensed uppercase), .serif-italic, .eyebrow, .mono-caps, .glass, .panel, .grid-bg, .vignette, .noise, .stat-bar (with inner <span style={{width}}/>). Fonts: font-[family-name:var(--font-display)] etc. are already mapped: use className "display" for headlines.
- Next 16 specifics: dynamic route params are Promises: `export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; ... }`. generateStaticParams works normally. Route handlers: `export async function POST(req: Request) { const body = await req.json(); return Response.json(...) }`. Use next/link and next/image (images from /public only; add width/height or fill).
- Icons: lucide-react. Motion: framer-motion ("motion" components, whileInView, viewport once). Charts: recharts (client only). Kanban: @dnd-kit/core + @dnd-kit/sortable. PDF: jspdf. Smooth scroll is global (Lenis); do not add another.
- Design: dark, cinematic, premium (see design-brief.md). Generous spacing, tabular numbers (className "tabular-nums"), no purple gradients, no generic three-equal-card slop unless the brief says so. Accessible: labels on inputs, buttons with text, keyboard focusable, aria where needed, color contrast ≥ 4.5:1 for body text.
- DO NOT run `next build` or `next dev` (shared .next directory, other agents are working). Validate with: `cd /home/user/EMPLOYER-SALARY-PREDICTION && npx tsc --noEmit` and `npx eslint <your files>`. Only errors in YOUR files matter (others may be mid-edit); yours must be clean before you finish.
- When done, reply with: the list of files you created, a 5-line summary of what works, and any contract you relied on from other agents (function names/paths).

YOUR FILES: src/app/page.tsx, everything under src/components/landing/, everything under src/components/three/, src/components/landing/data.ts (copy strings from the catalog you need), src/hooks/useScrollProgress.ts, src/hooks/usePointer.ts, src/hooks/useReducedMotion.ts.
TASK: build the cinematic 3D landing page exactly per docs/design-brief.md (sections 1-13 in that order) with real, working 3D. Read docs/ARCHITECTURE.md "Asset facts" for the GLB facts.
3D STACK: three + @react-three/fiber v9 + @react-three/drei v10 (React 19). Each scene is a client component that mounts <Canvas dpr={[1, 1.75]} gl={{ antialias: true, powerPreference: "high-performance" }} camera=...> and is loaded from the section via next/dynamic(() => import("./HeroScene"), { ssr: false, loading: () => <Poster/> }). Wrap model loaders in <Suspense> AND a small class ErrorBoundary that swaps to the procedural fallback if the GLB fails. useGLTF("/models/robot-head.glb") (meshopt decoder is enabled by default in drei; do not use Draco; call useGLTF.preload). Lights: ambient 0.25 + key directional (warm white, intensity 3, position 3,4,3) + rim spot (cyan #4ad9ff, intensity 6, from behind-left) + fill; environment from <Environment resolution={256}><Lightformer .../></Environment> with 2-3 Lightformers (NO preset, no HDR fetch). Use <ContactShadows> under the head. Tone mapping ACESFilmic (default). Keep frame work cheap: no per-frame React state, mutate refs in useFrame.
- RobotHead: center the model by bounding box (compute once in useLayoutEffect), scale to ~2.2 units tall, face +Z. Pointer look: lerp rotation.y toward pointer.x*0.45 and rotation.x toward -pointer.y*0.25 with damping; idle float sin(t)*0.04; slow breathing scale. Eye glow: traverse materials, and for meshes/material names containing "eye" or the brightest small emissive areas set emissive cyan; if none found, add two small emissive spheres at plausible eye positions (compute from bbox: y ≈ 0.62*height, x ±0.13*width, z at front). Scroll (progress 0→1 across the 220vh hero): rotation.y += progress*0.6, camera.position.z from 4.6 → 3.4, headline scale 1→1.15 & opacity 1→0, HUD panels translate out.
- ProceduralHead fallback: a stylized head from a sphere + jaw box + neck cylinders + chrome MeshStandardMaterial(metalness 1, roughness 0.25) with two emissive eye spheres. Must look intentional.
- WatchScene: sticky 300vh chapter; WatchModel from /models/watch.glb rotated x=+PI/2 (dial faces camera), centered, scaled ~2.4 units; camera path by scroll: p∈[0,.33] wide 3/4 view (camera 0.8,0.6,4.2 looking at 0,0,0; model yaw slowly), p∈[.33,.66] move to top-down (0,0.2,2.6), p∈[.66,1] macro push into the lower dial (target 0,-0.35,0; camera z 1.4, slight orbit). Gold key light (#ffd27a), cyan rim very low, drifting dust <Points> (600 particles, additive, gold, size 0.02) that slowly rise. Chapters (01 PARSING MODEL / 02 PRICING MODEL / 03 UPDATED DAILY) fade in/out with framer-motion based on progress ranges; eyebrow "PAYLENS · EDITION I — MARKET ENGINE"; corner meta.
- ProceduralWatch fallback: torus bezel + cylinder case + dial disc + rotating hands + tiny gear cylinders; gold materials.
- EmployeeScene ("Your profile, at work"): ProceduralEmployee — a stylized low-poly seated figure (head sphere, torso capsule, arms as capsules with elbow rotation, legs) at a desk (box) with a monitor (box with emissive cyan screen), typing animation (forearms bob alternately), breathing, head turns toward pointer; 4 floating glass cards (drei <Html transform occlude={false}> using the .glass style) that orbit slowly around the figure and tilt with pointer: "Product Strategy 85%", "$89K median", "Top 25%", "+12% skill-up". Backdrop: <Image>/img of /images/employee-backdrop.jpg if it exists (use an <img> with onError to hide) with a dark vignette overlay; if reduced motion, static poster.
- ScrollFilm: generic component that accepts { src: string | null, poster?: string, progress: MotionValue|number, children } — when src is present it renders a muted <video playsInline preload="auto"> and sets currentTime = progress*duration on scroll (throttled via rAF, seek coalescing); otherwise renders children. Read src/data/films.json for the film URLs (all null now).
- Hero HUD (see brief §1): left panel "MODULES S — A" list with tiles; right panel "$147K MARKET VALUE" + four stat rows with .stat-bar (widths animate on mount); bottom buttons "ANALYZE MY RESUME +" (href /analyze) and "START →" (href /auth/signup); corner meta. Headline layered behind the canvas (z-index) using .display text-[18vw] leading-none with the serif-italic "True".
- Other sections per the brief: stats strip (count-up), intent grid with 3D tilt cards (pointer-based rotateX/rotateY via framer-motion), how it works timeline, resume-as-center with animated bars/numbers, fixes-it-with-you, proof-of-work (studio backdrop, six module cards, CTA row), pricing teaser (use PLAN_META/PLAN_BULLETS), testimonials, FAQ accordion (use FAQ copy: import { FAQ } from "@/data/faq" — if that module is missing at your check time, keep a local fallback array with the six questions), final CTA. Copy comes from the catalog verbatim where the brief says so.
- page.tsx is a server component exporting metadata and composing the sections (sections that use hooks are client components).
- Mobile: HUD panels collapse into a bottom stack; headline scales; canvases still render but with dpr 1 and the watch chapter uses 200vh.
- Performance: lazy mount canvases with an IntersectionObserver (render only when near viewport), frameloop="demand" when off-screen, cap pixel ratio.
Verify with tsc + eslint on your files. Also run `node -e "require('fs').statSync('/home/user/EMPLOYER-SALARY-PREDICTION/public/models/robot-head.glb')"` to confirm assets exist.
