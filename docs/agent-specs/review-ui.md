# Review lens: ui

Review the UI of /home/user/EMPLOYER-SALARY-PREDICTION (src/app/**, src/components/**) against docs/design-brief.md and docs/payscope-feature-catalog.md: missing features vs the catalog (list each missing atomic capability with the route where it should live), plan-gating mistakes (features shown to Curious that require Explorer/Hunter or vice versa per src/lib/plans.ts), hydration hazards (store read during SSR without hydrated guard), accessibility (inputs without labels, icon-only buttons without aria-label, contrast, keyboard traps in kanban/accordion), broken links (href to routes that don't exist), copy inconsistencies (brand name "PayLens" everywhere, no "PayScope" branding in UI copy except in docs), responsive breakage (fixed widths, overflow). Grep for "TODO", "coming soon" placeholders that are not the intentional "Recruiter Mode (coming soon)". Report concrete findings with file:line and a proposed fix. Do NOT edit repo files.
