# Review lens: perf3d

Review /home/user/EMPLOYER-SALARY-PREDICTION/src/components/three/**, src/components/landing/**, src/hooks/** for 3D/motion robustness and performance: GLB load failure fallbacks actually wired (ErrorBoundary + Suspense), meshopt decoder usage (drei useGLTF default), no Draco/HDR CDN fetch, per-frame React setState (bad), scroll progress hooks leaking listeners, Lenis conflicts, Canvas mounted eagerly for every scene (should be lazy/near-viewport), dpr caps, reduced-motion handling, mobile layout of the HUD, memory (dispose on unmount), pointer handlers on window not cleaned up, framer-motion whileInView misuse, z-index of headline vs canvas, watch model orientation (dial must face camera: rotation.x = +PI/2), procedural fallbacks visually acceptable. Also verify `npm run build` output sizes for the landing route are reasonable and that the 4 MB GLBs are only fetched on the landing page. Report concrete findings with file:line and a proposed fix. Do NOT edit repo files.
