# Agent spec: engine


You are one of several engineers building "PayLens" in parallel inside /home/user/EMPLOYER-SALARY-PREDICTION (a Next.js 16.3 App Router app, React 19, TypeScript strict, Tailwind v4, src/ directory, "@/..." alias). Other agents are editing OTHER files at the same time.

READ FIRST (do not skip): /home/user/EMPLOYER-SALARY-PREDICTION/docs/ARCHITECTURE.md, /home/user/EMPLOYER-SALARY-PREDICTION/docs/design-brief.md, /home/user/EMPLOYER-SALARY-PREDICTION/docs/payscope-feature-catalog.md, /home/user/EMPLOYER-SALARY-PREDICTION/src/lib/types.ts, /home/user/EMPLOYER-SALARY-PREDICTION/src/lib/plans.ts, /home/user/EMPLOYER-SALARY-PREDICTION/src/lib/store.ts, /home/user/EMPLOYER-SALARY-PREDICTION/src/lib/engine/index.ts, /home/user/EMPLOYER-SALARY-PREDICTION/src/app/globals.css, /home/user/EMPLOYER-SALARY-PREDICTION/src/components/ui/index.ts (and the files it exports), /home/user/EMPLOYER-SALARY-PREDICTION/src/components/layout/Nav.tsx, /home/user/EMPLOYER-SALARY-PREDICTION/src/data/types.ts.

HARD RULES
- Only create/edit the files listed under YOUR FILES. Never edit files owned by others (types.ts, store.ts, plans.ts, globals.css, layout.tsx, Nav, Footer, ui primitives are FROZEN — if you truly need a new shared helper, create it inside your own folder).
- Write every file complete in one go; no placeholder TODOs, no "coming soon" stubs, no lorem ipsum. Every feature must actually work with the deterministic engine (no network needed).
- TypeScript strict: no `any`, no unused vars/imports (eslint is strict: eslint-config-next core-web-vitals + typescript). Prefer `import type`.
- Client components need "use client" at the top; anything using hooks, zustand (useApp), framer-motion, recharts, three, dnd-kit, window, or event handlers must be a client component. Server components are the default for pages that only render static content. Pages must export `metadata` only from SERVER components (so split a page into a server page.tsx that renders a client component when you need both).
- Zustand store persists to localStorage: read `hydrated` from useApp and render a small skeleton until true to avoid hydration mismatch. Use `useActiveAnalysis()` for the active resume + analysis.
- Money formatting: formatMoney(amount, currency) from "@/lib/format". Plan gating: <Gate feature="...">children</Gate> from "@/components/ui/Gate" and can(plan, feature) from "@/lib/plans".
- Tailwind v4 tokens available as classes: bg-bg, bg-bg-2, bg-panel, bg-panel-2, border-line, border-line-2, text-fg, text-muted, text-dim, text-cyan, bg-cyan, text-gold, bg-gold, text-ember, bg-ember, text-green, text-red, text-amber, rounded-xl2, shadow-glow-cyan, shadow-glow-gold, shadow-panel; utility classes: .display (Bebas condensed uppercase), .serif-italic, .eyebrow, .mono-caps, .glass, .panel, .grid-bg, .vignette, .noise, .stat-bar (with inner <span style={{width}}/>). Fonts: font-[family-name:var(--font-display)] etc. are already mapped: use className "display" for headlines.
- Next 16 specifics: dynamic route params are Promises: `export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; ... }`. generateStaticParams works normally. Route handlers: `export async function POST(req: Request) { const body = await req.json(); return Response.json(...) }`. Use next/link and next/image (images from /public only; add width/height or fill).
- Icons: lucide-react. Motion: framer-motion ("motion" components, whileInView, viewport once). Charts: recharts (client only). Kanban: @dnd-kit/core + @dnd-kit/sortable. PDF: jspdf. Smooth scroll is global (Lenis); do not add another.
- Design: dark, cinematic, premium (see design-brief.md). Generous spacing, tabular numbers (className "tabular-nums"), no purple gradients, no generic three-equal-card slop unless the brief says so. Accessible: labels on inputs, buttons with text, keyboard focusable, aria where needed, color contrast ≥ 4.5:1 for body text.
- DO NOT run `next build` or `next dev` (shared .next directory, other agents are working). Validate with: `cd /home/user/EMPLOYER-SALARY-PREDICTION && npx tsc --noEmit` and `npx eslint <your files>`. Only errors in YOUR files matter (others may be mid-edit); yours must be clean before you finish.
- When done, reply with: the list of files you created, a 5-line summary of what works, and any contract you relied on from other agents (function names/paths).


CANONICAL ROLE NAMES (use exactly these strings everywhere): Software Engineer, Frontend Engineer, Backend Engineer, Full Stack Engineer, Mobile Engineer, DevOps Engineer, Site Reliability Engineer, Cloud Architect, Security Engineer, QA Engineer, Data Engineer, Data Scientist, Machine Learning Engineer, AI Engineer, Data Analyst, Business Analyst, Product Manager, Technical Product Manager, Product Designer, UX Designer, UI Designer, Design Architect, Engineering Manager, Technical Support Engineer, Technical Support Specialist, Customer Service Specialist, Customer Service Lead, Integration Engineer, API Developer, Solutions Architect, Technical Sales, Sales Development Representative, Account Executive, Digital Marketing Specialist, Marketing Manager, Content Strategist, Operations Specialist, Operations Manager, Warehouse Operations Manager, General Manager, Project Manager, Program Manager, Scrum Master, HR Manager, Recruiter, Director of Administration, Financial Analyst, Accountant, Registered Nurse, Healthcare Administrator.
CANONICAL CITIES (label "City, CC"): US — San Francisco, New York, Seattle, Boston, Los Angeles, Chicago, Austin, Denver, Atlanta, Dallas, Miami, Washington DC, Remote (US). CA — Toronto, Vancouver, Montreal, Calgary, Ottawa, Remote (CA). UK — London, Manchester, Edinburgh, Birmingham, Bristol, Cambridge, Remote (UK). Currency: US→USD, CA→CAD, UK→GBP. Multipliers convert a USD national median into local nominal figures (e.g. San Francisco 1.42, New York 1.25, Seattle 1.18, Boston 1.12, Los Angeles 1.08, Chicago 1.02, Austin 1.0, Remote (US) 1.0, Toronto 1.08 CAD, Vancouver 1.04 CAD, Remote (CA) 0.98 CAD, London 0.82 GBP, Manchester 0.66 GBP, Remote (UK) 0.68 GBP — tune others sensibly).
Verified figures to reuse (from docs/payscope-feature-catalog.md §8): Software Engineer medians entry 86,000 / mid 115,000 / senior 145,000 / lead 176,000, 137,502 active roles, S/D 3.46; DevOps Engineer 83,473 / 113,967 / 135,746 / 176,000, 10,958 roles, S/D 1.9; API Developer S/D 0.32, 645 roles; other guide numbers are in the catalog.

YOUR FILES: src/lib/engine/analyze.ts, src/lib/engine/pricing.ts, src/lib/engine/scoring.ts, src/lib/engine/negotiation.ts, src/lib/engine/offer.ts, src/lib/engine/jobs.ts, src/lib/engine/pack.ts, src/lib/engine/roadmap.ts, src/lib/engine/insights.ts, src/lib/engine/recruiter.ts, src/lib/engine/rewrite.ts, src/lib/engine/coach.ts, src/lib/engine/text.ts (helpers), src/lib/engine/__tests__/engine.test.ts (Node test runner: `node --import tsx --test` is NOT available; instead write a plain script src/lib/engine/__tests__/smoke.ts that we can run with `npx tsx src/lib/engine/__tests__/smoke.ts` — install tsx as a devDependency if missing via `npm i -D tsx --no-audit --no-fund`).
The seed data now exists in src/data/*.ts (roles.ts ROLES, cities.ts CITIES/COUNTRY_META/FX_TO_USD, skills.ts SKILLS, certs.ts CERTS, courses.ts COURSES, industries.ts INDUSTRIES, jobs.ts JOBS, guides.ts GUIDES) and helpers in src/lib/engine/catalog.ts — READ THEM and build on them. Replace the STUB files; keep every exported function name/signature that src/lib/engine/index.ts re-exports (parseResume, analyzeResume, evaluateOffer, compareOffers, searchJobs, checkFit, getJob, listJobs, buildApplicationPack, buildGrowthPlan, getInsights, benchmarkJobDescription, rewriteBullet, rewriteBullets, coachReply). All pure TypeScript, deterministic (seeded pseudo-randomness via hash() from @/lib/utils only), no React, no network.
REQUIREMENTS
1. parseResume(text, {displayName}) → Profile: strip emails/phones/URLs (ignored for pricing); detect titles (lines near the top and "Experience" sections; map with findRole), seniority from title words (intern/junior/associate → entry; "senior"/"sr" → senior; "lead/staff/principal/head/director/vp" → lead; else by years), years of experience from date ranges (e.g. "2019 – Present", "Jan 2017 - Mar 2021") and phrases ("7 years"), skills via SKILLS aliases + inferred skills from context (e.g. mentions of "A/B tests" → Experimentation; "roadmap" → Product Strategy) rated Expert/Advanced/General/Inferred by frequency + section placement with confidence 40-98, education (degrees/schools), certifications (match CERTS), employment type, employer type (startup/enterprise/agency/public sector), industry (INDUSTRIES aliases), location (city names / "Remote"), remote preference, summary (first paragraph), bullets (lines starting with -, •, or verbs), wordCount. Handle pasted job descriptions too (treat as profile of the role).
2. pricing.ts estimate(profile, opts?) → MarketEstimate: base = ROLES[role].medians[level]; multiply by city multiplier and industry multiplier; skill premium = clamp(Σ driver×weight over the profile's rated skills normalized, -12%..+22%); experience curve inside the level; remote adjustment (-2% US remote vs national, region-specific); floor = median×p25, ceiling = median×p75; percentile from where the skill-adjusted median sits vs the role's level band (map -12%→15th … +22%→92nd) + label via percentileLabel(); skillsLifting/skillsDragging (top 5 each by contribution in currency); skillUpPotentialPct = uplift if the 3 highest-demand missing core skills were added (5-18%); dataPoints deterministic per role (e.g. activeRolesUS×73); updatedAt = today.
3. scoring.ts score(profile, estimate) → ResumeScore: 0-100 from (quantified achievements density, action verbs, skills coverage vs role coreSkills, length 350-900 words, summary presence, education, certifications, leadership scope, AI/ML tooling mention, recency, contact-free) and Improvement[] with HIGH/MED/LOW severity and impact ranges matching the catalog examples ("Missing quantified achievements" +8–12%, "No mention of AI/ML tooling" +6–9%, "Education section too sparse" +3–5%, "No leadership scope indicated" +4–7%, plus others like "Skills list missing high-demand core skills", "Summary doesn't state level/role", "Bullets are duties, not outcomes", "Resume too long/short"). Also strengths (top skills with demand) and gaps (missing core skills with fix + impact).
4. negotiation.ts brief(estimate, profile) → NegotiationBrief: floor = median (round to 500), target = median×1.06, stretch = ceiling; openingScript in first person mentioning years, top 2 skills, city, market median/range and the target ask (pattern from the catalog); 3 counterTactics (sign-on/equity if base frozen; earlier review; remote/title); leverage notes ("top X% of candidates for this stack" from percentile); talkingPoints from strengths; totalPotentialGain = stretch - floor.
5. offer.ts evaluateOffer(offer, profile?) → OfferVerdict: level from title/profile; market from ROLES + city; verdict below/at/above using ±5% band around median; pctVsMedian rounded; percentile of base within floor..ceiling; total comp = base + bonus + equity + signOn vs marketTotalComp (median × 1.12 for tech roles); negotiation targets for THIS offer (floor "don't go below" = max(offer.base, median×0.94), target = median×1.03 or offer×1.1 whichever is higher but ≤ ceiling, stretch = ceiling×0.95); offer-specific script; breakdown rows; decisionScore 0-100 (total comp vs market, growth, remote, equity) ; notes. compareOffers ranks by decisionScore with reasons.
6. jobs.ts searchJobs(query, profile?) → JobMatch[] filtered by role fuzzy match, location/country, minSalary, remote, free text; sorted by fit desc. checkFit(job, profile) → JobFit: requirement matrix (matched / partial / gap) by matching each requirement sentence to profile skills/years/level (years regex, skill aliases, domain words), score = weighted; marketMedian for job role+city; salaryContext string comparing job midpoint to the profile's median.
7. pack.ts buildApplicationPack(job, profile) → tailored summary (2-3 sentences using job title/company + top matched skills), 4-6 tailored bullets (rewrite profile bullets to include job keywords + quantification prompts), cover letter (4 paragraphs, company-specific hook from job.description/industry), 6 interview prep Q&As (behavioral + role-specific, STAR outlines).
8. roadmap.ts buildGrowthPlan(profile, target) → ladder from ROLES ladder with medians in the profile's currency, months to target (based on salary gap and level jump), steps: missing coreSkills ordered by demand×driver with weeks, uplift, cumulativeSalary and 1-2 COURSES each, certifications ranked by uplift×relevance for the target role. Also export certificationsFor(role, city, profile) and roadmapFor(profile, targetRole?) used by analyzeResume.
9. insights.ts getInsights(role, location, profile?) → 12-month trend (deterministic seeded walk around the median), supplyDemandRatio + marketLabel thresholds (see ARCHITECTURE.md), activeRoles, emergingSkills (top trend skills for the role), decliningSkills, remotePremiumPct, cityPremiums (8 cities), countryComparison (US/CA/UK medians in local currency + USD via FX_TO_USD).
10. recruiter.ts benchmarkJobDescription({title, description, location, industry}) → RecruiterReport: role/level from text, median, range, byCity (San Francisco, New York, Austin, Seattle, Remote + the requested city), byLevel (Junior/Mid-Level/Senior/Lead), byIndustry (Technology, Finance, Healthcare, E-commerce), requiredSkills/niceToHave extracted, recommendedBand (P40–P65), notes.
11. rewrite.ts rewriteBullet(bullet, profile?) → stronger bullet: start with an action verb, add scope/quantification placeholder like "(X%)" only if none present, include market language for the role; reasons list.
12. coach.ts coachReply(message, ctx) → helpful deterministic coaching answer that references the user's actual analysis (score, top gaps, target numbers) and answers intents: "rewrite my summary", "what should I fix first", "am I underpaid", "how do I negotiate", "which certification", "tailor for <company>", generic fallback with 3 concrete next steps.
13. analyze.ts analyzeResume(input) composes: parse → estimate → score → brief → certifications → roadmap; returns Analysis with id = "a_"+hash(text). Must run in <50ms for a 900-word resume.
14. smoke.ts: runs analyzeResume on a built-in 600-word sample Senior Product Manager (Los Angeles) resume and asserts: median between 120k and 190k USD, percentile 40-95, ≥8 skills detected, score 40-90, ≥3 improvements, brief target > floor, evaluateOffer verdict for {title:"Senior Product Manager", location:"Austin, US", base:105000} is "below", searchJobs({role:"Product Manager"}) returns ≥5 matches with fit 30-100, getInsights("Software Engineer","San Francisco, US").marketLabel === "Competitive", benchmarkJobDescription(...) returns byCity length ≥5. Print PASS/FAIL lines and exit non-zero on failure. Run it and make it pass.
