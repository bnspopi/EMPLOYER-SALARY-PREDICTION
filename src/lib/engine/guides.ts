import type { SalaryGuide } from "../types";
// STUB — replaced by the engine implementation. Keep signatures.
export function listGuides(): SalaryGuide[] { return []; }
export function getGuide(slug: string): SalaryGuide | undefined { void slug; return undefined; }
